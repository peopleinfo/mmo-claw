{
  "name": "@mmo-claw/actors",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "pnpm exec tsc -p tsconfig.json --watch",
    "build": "pnpm exec tsc -p tsconfig.json",
    "lint": "pnpm exec eslint \"src/**/*.ts\"",
    "test": "pnpm exec vitest run src/instagram-poster/actor.test.ts"
  },
  "dependencies": {
    "@mmo-claw/browser": "workspace:*",
    "@mmo-claw/db": "workspace:*",
    "zod": "^3.24.2"
  }
}
