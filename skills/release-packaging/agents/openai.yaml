interface:
  display_name: release-packaging
  short_description: Build signing, auto-update, and cross-platform packaging pipelines. Use for release readiness and distribution workflows.
  default_prompt: Use the release-packaging workflow to complete this task.

